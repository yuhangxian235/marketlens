# Network manifests

Phase 1 creates `local-monad-fork.json` with chain ID, RPC label, mandatory upstream fork block number and expected hash, required trace methods and explorer metadata. Startup must verify the fork header before deployment. No private key belongs here.

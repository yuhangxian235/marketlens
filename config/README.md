# Configuration ownership

Network parameters and deployment addresses live here as generated/validated manifests. Python, TypeScript and Solidity scripts must consume the same files instead of copying addresses into source.


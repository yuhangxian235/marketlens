# Analytics tests

当前覆盖：

- RPC 日志幂等与复跑；
- ABI 解码失败；
- composite event key 去重；
- UTC 日期与 partial-day；
- first-seen 唯一性；
- D1 eligibility；
- SQL / pandas 一致性；
- exact wei 与展示 MON 的精度守栏。
- registered question routing 与查询 deadline；
- evidence sample transaction 的 analysis-run 隔离。

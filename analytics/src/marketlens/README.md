# MarketLens analytics package

Contains the RPC adapter, ABI decoder, idempotent/reorg-aware indexer, analytics pipeline,
independent validation, evidence catalog, registered question router, and CLI.

# Project entrypoints

PowerShell entrypoints and equivalent Make targets cover:

- bootstrap tool/dependency checks;
- start/reset Monad Anvil fork;
- build/test/deploy/seed;
- index/analyze/validate/export;
- start the web app;
- run the complete demo preflight.

Primary commands are `bootstrap.ps1` and `demo.ps1`.

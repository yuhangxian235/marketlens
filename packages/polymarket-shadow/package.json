{
  "name": "@marketlens/polymarket-shadow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsup src/index.ts --format esm --dts --sourcemap --clean --target es2022",
    "test": "vitest run"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.0"
  }
}
